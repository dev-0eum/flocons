# 03 — 관측성 (Observability)

> Operations 산출물 #3. **로깅 · 크래시 리포팅 · 핵심 학습 지표(분석)** 를 정의하되, **프라이버시 경계**를 먼저 못 박는다.
> 작성: **cloud-dev** 리드 mob (qa-dev · code-review, 필요 시 back-dev).
> 근거: [docs/DESIGN.md](../../docs/DESIGN.md) §4·§5 · [docs/HARNESS.md](../../docs/HARNESS.md) §5.
> `<...>`는 자리표시자, `TODO`는 채워야 할 항목이다. 채운 뒤 표시자와 본 안내 블록을 정리한다.
> **실제 SDK/도구 도입은 [README.md](README.md)의 승인 체크포인트 이후에만 한다.**

작성일: `<YYYY-MM-DD>` · 작성: cloud-dev + Operations mob · 상태: `<⬜ Pending | 🔵 In Progress | ⏸️ Awaiting Approval | ✅ Approved | 🔁 Changes Requested>`

---

## 1. 한 줄 요약

`<무엇을 관측해서 어떤 결정/대응을 할 것인지 한 문장 — 그리고 무엇은 절대 수집하지 않는지>`

## 2. 프라이버시 경계 (먼저 정한다)

> 개인 사용 앱이고 온디바이스가 기본이다. **수집 최소화**가 원칙.

- ❌ **절대 수집/전송 금지**: API 키·토큰, 사용자가 입력한 비밀, 학습 단어 내용/문장 원문, 식별 가능한 개인정보.
- ✅ **수집 가능(익명·집계)**: `<예: 크래시 스택, 익명 사용 카운트, 성능 타이밍>`
- 옵트인/옵트아웃: `<예: 분석은 설정에서 끌 수 있게 / 기본 off>`
- TODO: `<원격 전송 여부 자체를 둘지 결정 — 안 보내면 로컬 로깅만>`

## 3. 로깅 (Logging)

| 레벨 | 용도 | 예 |
|---|---|---|
| error | `<복구 불가/예외>` | `<예: Provider 폴백 실패>` |
| warn | `<폴백·재시도>` | `<예: AI Provider 오류 → Static 폴백>` |
| info | `<주요 흐름>` | `<예: 덱 로드, 복습 큐 생성>` |
| debug | `<개발 전용>` | `<프로덕션 비활성>` |

- 구현 위치: `<예: lib/log.ts 래퍼 — DESIGN §8 구조의 lib/>`
- 규칙: `<비밀/개인정보 마스킹, 프로덕션 레벨 제한>`
- TODO: `<로그 보관/회전(원격 전송 시)>`

## 4. 크래시 리포팅 (Crash)

- 도구: `<예: Expo/EAS 제공 기능 / Sentry / 사용 안 함>`
- 범위: `<예: JS 예외 + 네이티브 크래시>`
- PII 스크럽: `<수집 전 민감정보 제거 규칙>`
- TODO: `<릴리스 버전 태깅, 소스맵 업로드>`

## 5. 핵심 분석 지표 (Metrics)

> 제품 흐름([docs/DESIGN.md](../../docs/DESIGN.md) §2)과 직접 연결된 **집계 가능한** 지표만.

| 지표 | 정의 | 왜 보는가 |
|---|---|---|
| `<예: 세션당 분류 카드 수>` | `<...>` | `<예: 학습량/세션 길이>` |
| `<예: SRS 복습 도달률>` | `<due 중 실제 복습 비율>` | `<예: 리텐션 신호>` |
| `<예: Provider 폴백률>` | `<AI→Static 폴백 발생 비율>` | `<예: AI 안정성>` |
| `<예: 크래시-프리 세션율>` | `<...>` | `<예: 안정성 게이트>` |
| TODO | | |

- 저장/집계: `<예: 로컬 집계만 / 익명 원격>`
- TODO: `<지표 추가 — 단, 2절 프라이버시 경계 준수>`

## 6. 알림 / 대시보드 (있으면)

- 알림: `<예: 크래시-프리율 임계 이하 시 — 누구에게/어떻게>`
- 대시보드: `<예: EAS/도구 콘솔 링크>`
- 사고 연결: `<임계 초과 → 04-runbook 사고 대응 진입>`
- TODO: | |

## 7. 리스크 / 미해결 질문

| ID | 항목 | 영향 | 완화책 / 결정 필요 |
|---|---|---|---|
| OPS-OBS-01 | `<예: 원격 전송 시 프라이버시 노출>` | `<상/중/하>` | `<예: 전송 안 함 또는 익명·옵트인>` |
| TODO | | | |

---

> 다음 산출물: [04-runbook.template.md](04-runbook.template.md). 이전: [02-cicd.template.md](02-cicd.template.md).
> 이 문서는 Operations 변경 체크포인트에서 사람 승인 대상이다. [README.md](README.md) 참고.
